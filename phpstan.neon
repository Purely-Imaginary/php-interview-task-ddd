parameters:
    level: 6
    paths:
        - src
        - tests
        # Add bin/calculate-fee if you want to analyze it, might need bootstrap setup
    ignoreErrors:

    # If your bin script needs bootstrapping for analysis (e.g., for DI):
    # bootstrapFiles:
    #    - bin/bootstrap.php # We'll create this later for DI